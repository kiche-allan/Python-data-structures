numbers = [3, 4, 7, 2, 9, 4, 6, 7]

max = numbers[0]

# we loop through all the numbers to check the values

for number in numbers:

    # if the number is greater than max, then we reset max to number

    if number > max:

        # reset max to


        max = number

print(max)