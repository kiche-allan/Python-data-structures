def square(number):
     return number * number
 # return number * number
 # by default, all functions in python return a none
  #so u can return the result using the return statement


print(square(3))
